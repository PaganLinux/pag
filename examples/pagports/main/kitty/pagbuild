# PaganLinux Build Script (pagbuild)
# GPU-accelerated terminal emulator

maintainer="PaganLinux Team <team@paganlinux.eu>"
pkgname=kitty
pkgver=0.35.2
pkgrel=0
pkgdesc="GPU-accelerated terminal emulator"
url="https://sw.kovidgoyal.net/kitty/"
arch="x86_64"
license="GPL-3.0-only"
makedepends="gcc make python3 harfbuzz-dev freetype-dev fontconfig-dev wayland-dev libx11-dev libxcb-dev"
depends="harfbuzz freetype fontconfig wayland libx11 libxcb"
source="https://github.com/kovidgoyal/kitty/archive/v0.35.2.tar.gz"
builddir="$srcdir"

build() {
    make
}

package() {
    make DESTDIR="$pkgdir" install
}

# sha512sums=""
